spring:
  cloud:
    config:
      token: roottoken
  config:
    import: configserver:http://localhost:8888
  application:
    name: ingredient-service

management:
  info:
    git:
      mode: full
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'