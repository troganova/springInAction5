server:
  port: 0

spring:
  application:
    name: ingredient-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.0
